<!DOCTYPE html>
<html lang="en">
  <%- include('../include/head') %>
  <body>
    <%- include('../include/header') %>
    <main class="p-8 flex flex-col gap-4 items-center justify-center">
      <h1 class="text-2xl mb-4 text-slate-700"><%= prod.title %></h1>
      <hr class="border-cyan-500 w-full" />
      <img class="size-96" src="<%= prod.img %>" alt="<%= prod.title %>" />
      <h2 class="text-2xl font-bold text-slate-700">Rs.<%= prod.price %></h2>
      <p class="text-lg font-light text-slate-700"><%= prod.description %></p>
      <form action="/cart" method="post">
        <button
          class="py-2 px-4 rounded text-md text-slate-white bg-slate-500 hover:bg-slate-600 hover:cursor-pointer hover:scale-110 transition active:scale-75"
        >
          Add to cart
        </button>
      </form>
    </main>
  </body>
</html>
