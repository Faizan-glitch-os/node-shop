<!DOCTYPE html>
<html lang="en">
  <%- include('../include/head'); %>
  <body class="p-0 m-0 font-san">
    <%- include('../include/header') %>
    <main class="w-[100vw] h-[100vh] flex flex-col items-center">
      <% if (prod.length > 0) { %>
      <div class="p-9 grid sm:grid-cols-1 md:grid-cols-3 gap-2">
        <% prod.forEach(element => { %>
        <article
          class="w-96 shadow-lg shadow-cyan-900 flex flex-col gap-1 items-center justify-center"
        >
          <h2
            class="mt-8 text-3xl text-black font-bold mb-4 mr-4 ml-4 text-center"
          >
            <%= element.title %>
          </h2>
          <img class="h-96 w-96" src="<%= element.img %>" />
          <div class="m-4 flex flex-col items-center">
            <p class="text-2xl font-bold text-slate-700">
              Rs.<%= element.price %>
            </p>
            <p class="text-lg font-light text-slate-700">
              <%= element.description %>
            </p>
            <div>
              <form
                class="inline"
                action="/admin/delete-product/<%= element.id %>"
                method="post"
              >
                <button
                  type="submit"
                  class="py-2 mr-2 px-4 mt-8 rounded border border-slate-500 text-md text-slate-white hover:cursor-pointer hover:scale-110 transition active:scale-75"
                >
                  Delete
                </button>
              </form>
              <a href="/admin/edit-product/<%= element.id %>?editing=true">
                <button
                  type="submit"
                  class="py-2 px-4 rounded text-md text-slate-white bg-slate-500 hover:bg-slate-600 hover:cursor-pointer hover:scale-110 transition active:scale-75"
                >
                  Edit
                </button>
              </a>
            </div>
          </div>
        </article>

        <% })%>
      </div>
      <% } else { %>
      <p class="text-2xl text-slate-900">No products available yet</p>
      <% } %>
    </main>
  </body>
</html>
